export type Movie = {
    id: number;
    title: string;
    backdrop_path: string;
    poster_path: string;
}

export type Tag = {
    id: number;
    name: string;
}

export type Actor = {
    id: number;
    name: string;
    profile_path: string;
}